# Копируйте в .env и задайте значения. Не коммитьте .env в репозиторий.

DJANGO_SECRET_KEY=dev-key-change-in-production
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

FIREBIRD_DB_PATH=mz.fb
FIREBIRD_USER=SYSDBA
FIREBIRD_PASSWORD=masterkey

# Режим «мост»: если основной процесс — 64-bit Python, а Firebird/UDF только 32-bit,
# оставьте пустым в процессе-мосте (32-bit) и задайте в основном (64-bit), например:
FIREBIRD_BRIDGE_URL=http://127.0.0.1:8765
# FIREBIRD_BRIDGE_URL=
